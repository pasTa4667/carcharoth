"""Isolated strategy quick tests: bars -> strategy -> in-memory results.

No engine, no regime detection, no risk management. Each symbol is simulated
independently; everything stays in memory and is persisted once at the end.
"""
